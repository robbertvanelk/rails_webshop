<div class="page-content">
	<% @products.each do |p| %>
		<div class="product">
			<p><%= link_to p.name, product_index_path(:id => p.id) %></p>
			<p>€ <%= p.price %></p>
			<%= image_tag p.photo %>
		</div>
	<% end %>
</div>